import Single from '../../../components/Single';

export const meta = {
	title: 'GretaThemes',
	tag: 'WordPress',
	description: 'Free and premium WordPress themes that are clean, simple and just work.',
	thumb: 'gretathemes/thumb.jpg',
};

export default ({ children }) => <Single meta={meta}>{children}</Single>;
